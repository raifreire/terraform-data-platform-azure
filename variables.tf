# Variáveis reservadas para uso futuro na raiz
# (não obrigatórias no momento)
variable "example" {
  description = "Placeholder variable to maintain structure"
  type        = string
  default     = null
}
